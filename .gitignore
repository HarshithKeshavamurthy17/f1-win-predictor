# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
data_collection.log

# Temporary files
*.tmp
temp/
tmp/

# Don't commit large data files (we'll regenerate them)
# But keep the directory structure
data/raw/*.parquet
data/raw/*.csv
!data/raw/.gitkeep

# Keep models directory but not the large model files
# BUT allow predictions file (small and needed for app)
data/curated/models/*.parquet
!data/curated/models/preds.parquet
!data/curated/models/.gitkeep

# Keep curated data files (small and needed for app)
!data/curated/f1_training.parquet
!data/curated/f1_training_base.parquet
!data/curated/races.parquet
!data/curated/driver_race.parquet


